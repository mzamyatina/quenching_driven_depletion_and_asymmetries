from pathlib import Path

path_to_constants = Path.home() / "scripts" / "constants"
path_to_data_umserve = Path.home().parent.parent / "data" / "mz355"
path_to_datasets = Path.home() / "datasets"
path_to_observations = Path.home() / "observations"
path_to_processed = Path.home() / "processed"
